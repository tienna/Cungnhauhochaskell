-- Create a one-line function that filters `cars` by brand and then checks if there are any left.
-- cars = [("Toyota",0), ("Nissan",3), ("Ford",1)]
-- timcar = filter (\(_,x) -> x > 0) cars

timcar cars= filter (\(_,x) -> x > 0) cars

 
